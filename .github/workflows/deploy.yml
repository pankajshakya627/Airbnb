name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull latest image
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      # ─────────────────────────────────────────────
      # DEPLOYMENT OPTIONS (uncomment one)
      # ─────────────────────────────────────────────

      # Option 1: Deploy via SSH to a VPS
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.DEPLOY_HOST }}
      #     username: ${{ secrets.DEPLOY_USER }}
      #     key: ${{ secrets.DEPLOY_SSH_KEY }}
      #     script: |
      #       docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
      #       docker compose -f /opt/airbnb/docker-compose.prod.yml up -d
      #       docker image prune -f

      # Option 2: Deploy to AWS ECS
      # - name: Deploy to ECS
      #   uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      #   with:
      #     task-definition: task-definition.json
      #     service: airbnb-service
      #     cluster: airbnb-cluster

      # Option 3: Deploy to Railway / Render / Fly.io
      # - name: Deploy to Railway
      #   run: railway up --service airbnb-api

      - name: Deployment placeholder
        run: |
          echo "✅ Docker image ready: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo "📝 Configure deployment by uncommenting one of the options above."
          echo "🔗 Image can be pulled with: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"

  health-check:
    name: ❤️ Health Check
    runs-on: ubuntu-latest
    needs: deploy
    if: ${{ false }}  # Enable when deployment is configured

    steps:
      - name: Wait for deployment
        run: sleep 30

      - name: Check health endpoint
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.DEPLOY_URL }}/)
          if [ "$STATUS" != "200" ]; then
            echo "❌ Health check failed with status $STATUS"
            exit 1
          fi
          echo "✅ Health check passed!"
